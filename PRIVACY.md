# Privacy

StorageScope processes file names, paths, sizes, timestamps, and optional duplicate hashes locally on the Mac during user-requested scans.

The app does not upload scan results, file contents, analytics, or identifiers. Recent scan access is stored locally using app-scoped security-scoped bookmarks so the app can reopen folders the user previously granted.

Cleanup actions require confirmation and move files to Trash through macOS APIs.
